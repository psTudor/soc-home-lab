# SQL Injection OWASP JuiceShop


## General Information

| Field | Details |
|---|---|
| Date | 28.06.2026 |
| Start time (UTC) | 02:07 PM UTC |
| End time (UTC) | 02:59 PM UTC |
| Tool used | e.g. SQLmap, Burp Suite, Browser |
| OWASP Top 10 | e.g. A03:2021 - Injection |
| MITRE ATT&CK | e.g. T1190 - Exploit Public-Facing Application |

---

## Brief Description

SQL Injection allows an attacker to insert malicious SQL code
into the input fields of a web application. If the input is not sanitized,
the database executes the injected code, potentially leading to data
exposure or authentication bypass.

---

## Steps Taken

1. Launched Burp Suite and configured proxy on Firefox
2. Accessed http://[VM-IP]:80/#/login
3. Captured the login request in HTTP History
4. Ran SQLmap with the following command:

```bash
sqlmap -r ~/login.txt \
       --level=3 \
       --risk=2 \
       --dbms=sqlite \
       --batch \
       --ignore-code=401 \
       --dbs
```

5. SQLmap confirmed the vulnerability and identified the database backend
6. Performed manual authentication bypass using payload: `' OR 1=1--`
7. Recorded end time and moved to Sentinel for detection analysis

---

## Attack Screenshots

<!-- Save screenshots in the screenshots/ folder and update filenames accordingly -->
![SQLmap Output](/screenshots/sqli_sqlmap.png)
*SQLmap output confirming boolean-based blind SQL Injection vulnerability*

![SQLi Payload in Login Form](/screenshots/sqli_payload_login.png)
*Manual payload entered in the email field: ' OR 1=1--*

![Logged in as Admin](/screenshots/sqli_admin_logged.png)
*Successful authentication bypass logged in as administrator without valid credentials*

---

## Detection in Sentinel

### Detection Results

| Metric | Value |
|---|---|
| Attack detected? | Yes, via NginxAccess_CL status code pattern  |
| Time to detection | Manual KQL query, no automated rule configured  |
| Total login requests generated | 548 |
| Requests with SQLi patterns detected | 4 |
| False positives | None |

### Sentinel Screenshots

![KQL Results in LAW](/screenshots/sqli_KQL.png)
*SQLi payload patterns in NginxAccess_CL*

![Attack Timeline](/screenshots/sqli_timeline.png)
*Request volume per minute showing attack pattern during the session*

![Total login](/screenshots/sqli_login_requests.png)
*Total login requests*

![SQLI detected](/screenshots/SQLI_detected.png)

---

## Conclusion

Out of 441 requests generated by SQLmap, only 4 contained SQLi 
patterns visible in Nginx logs. This is because Nginx logs the 
request URL but not the HTTP request body, where most SQLi payloads 
are transmitted via POST. Full payload visibility would require 
a WAF such as ModSecurity.

